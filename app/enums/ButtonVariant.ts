export enum ButtonVariant {
  /** Filled accent. One per view. */
  PRIMARY = 'primary',
  /** Outlined. Sits beside a primary without competing. */
  SECONDARY = 'secondary',
  /** Text link with an arrow. Keeps tertiary actions from adding chrome. */
  TERTIARY = 'tertiary',
}
